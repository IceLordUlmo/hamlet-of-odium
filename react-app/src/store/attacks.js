// imports

// action types

// action creators

// thunk creators

// initial state
const initialState = {};
// reducer

const Reducer = (state = initialState, action) => {
    let newState;

    switch (action.type) {
        default:
            return state;
    }
}

export default Reducer;